from pbkdf2 import PBKDF2


def tryusername(username, hash):
    #count the number of letters in username
    #numloops = pow(2, len(username))
    for i in len(username):
        
        print(i) 

#tryusername("aaaa", "aaaa")